import { CvcMaskDirective } from './cvc-mask.directive';

describe('CvcMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CvcMaskDirective();
    expect(directive).toBeTruthy();
  });
});
