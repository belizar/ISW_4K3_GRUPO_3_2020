import { VencMaskDirective } from './venc-mask.directive';

describe('VencMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new VencMaskDirective();
    expect(directive).toBeTruthy();
  });
});
